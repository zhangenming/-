{
  "name": "dispatch-settlement-app",
  "version": "1.0.0",
  "private": true,
  "description": "本地 Node.js 服务 + JSON 持久化",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "PORT=2999 APP_ENV=development node --watch-path=server.js server.js",
    "start": "PORT=3000 APP_ENV=production node server.js"
  }
}
