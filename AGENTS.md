# 项目级说明

<!-- - 不要自动执行npm run build, 由我手动执行 -->
- 现在每次buildNumber新增时  自动执行npm run build

- 生产数据在 `data/` 目录，操作时要谨慎。
- 测试和开发优先使用 `data-dev/`。
- 未经明确要求，不要删除记录、备份、图片、登录会话或鉴权文件。1
- 修改数据持久化逻辑时，要兼容已有 JSON 记录。

版本号逻辑 ：
- 每次新增新功能： x + 1 1
- 每次修复或优化： y + 1
- 每次 build： buildNumber （z） + 1
- x/y和任务相关, z和build相关
- changelog里的版本号是x.y.z的格式
- time要保证是当前北京时间, 不要编造成未来的
- 每次更新代码 都向CHANGELOG.json文件写入更新日志, 包括时间,版本号,具体内容和更新类型(新功能/修复/优化) 时间一定要对 用现在的北京时间