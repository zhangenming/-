# 项目级说明

- 不要自动执行npm run build, 由我手动执行
<!-- - 现在每次buildNumber新增时  自动执行npm run build -->

- 生产数据在 `data/` 目录，操作时要谨慎。
- 测试和开发优先使用 `data-dev/`。
- 未经明确要求，不要删除记录、备份、图片、登录会话或鉴权文件。

版本号逻辑 ：
- 每次新增新功能： x + 1, y不清零
- 每次修复或优化： y + 1
- 每次 build： buildNumber （z） + 1
- x/y和任务相关, z和build相关
- changelog里的版本号是x.y.z的格式
- 每次更新代码 都向CHANGELOG.json文件写入更新日志, 包括时间,版本号,具体内容和更新类型(新功能/修复/优化) 时间一定要对 用现在的北京时间(结束时的时间),不要编造成未来的日期
- 每轮对话过后 都提交一次git(不提交data目录下的文件), git message 为这次对话我的prompt


会计界面 一定不能看到付款价和溢价! 导出也不行! 意思就是会计角色不能看到付款价和溢价
会计界面 不能看到备注



5月27日前注册的会计 会计结算价的默认比例是60%  之后默认的是50%
6月1日后 全是50%了



接待收益 = 溢价收益 + 基础收益

溢价收益：
按溢价合计走阶梯公式 45% / 50% / 55%

基础收益：
普通单 = 会计价 * 8%
月结单 = 会计价 * 13%


阶梯公式:
2026-06-01 前订单：老公式
0-1000：45%
1000-2000：50%
2000以上：55%

2026-06-01 起订单：新公式
0-3000：40%
3000-5000：45%
5000以上：50%


所有凡是涉及到的接待名排序的地方
统一按: 开心财税 acdek1 k旧1旧 这个顺序排序



http://localhost:2999               本地代码    本地数据
http://localhost:3000               本地代码    生产数据
https://kaixincaishui.cloud/        生产环境    生产数据
