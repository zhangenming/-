## 目标
- 将右侧“学员档案”模块像示例图一样一比一还原：外层米色卡片背景、顶部标题+“编辑”链接、内部白色信息卡片（圆角、分割线）、头像、两列标签-值排版、最后一行显示“12节 (100%)”。

## 页面与文件
- 主要文件：`src/students.vue`（已经存在学员管理页面草稿）
- 辅助：复用 `admin.vue` 的主题色变量，不额外改动其它页面

## 结构改造
1. 将现有 `.profile-card` 拆分为：
   - 外层：`.profile-card`（米色背景，轻阴影）
   - 内层：`.profile-inner`（白底信息卡，圆角更大，包含头像与信息表）
2. 标题区：`.profile-header` 左侧标题“张雨薇的学员档案”，右侧“编辑”链接（蓝色，hover 下加下划线）。
3. 头像：居中圆形头像（72px），下方开始信息表格。
4. 信息表：五行两列布局，左列标签、右列值；每行之间加分割线；行高一致。
   - 行1：学员ID | 值
   - 行2：姓名 | 值
   - 行3：当前状态 | 值
   - 行4：注册日期 | 年-月-日（按示例分段显示）
   - 行5：已完成课程 | “12节 (100%)”（用真实计算填充）

## 样式规范（精细数值）
- 外层卡片 `.profile-card`
  - 背景：`#FEF3C7`（amber-100，接近示例米色）
  - 圆角：`16px`
  - 阴影：`0 4px 12px rgba(0,0,0,.06)`
  - 内边距：`16px`
- 标题区
  - 标题颜色：`#111827`，字重 700，字号 `16px`
  - 编辑链接颜色：`#3B82F6`，字号 `14px`，`hover: text-decoration: underline`
- 内层 `.profile-inner`
  - 背景：`#fff`
  - 圆角：`16px`
  - 内边距：`16px`
  - 分割线颜色：`#E5E7EB`
- 头像 `.avatar`
  - 尺寸：`72px`，居中显示，`margin: 8px auto 12px`
- 信息表 `.profile-table`
  - 行高：`40px`
  - 行间分割线：`border-bottom: 1px solid #E5E7EB`
  - 左列（标签）颜色：`#6B7280`，字号 `13px`
  - 右列（值）颜色：`#111827`，字重 600，字号 `14px`
  - 组装日期为三段：`YYYY- MM- DD`（使用 CSS `letter-spacing` 或在模板中插入分段）

## 逻辑与数据
- 计算完成率：`completion = Math.round(doneLessons / totalLessons * 100)`
- 显示为：`已完成课程 12节 (100%)`
- 暂时使用假头像（示例圆形灰色），后续可替换为真实 URL

## 代码改动（概述）
- `src/students.vue`：
  1. 模板：在 `aside.profile` 中引入 `.profile-inner` 与 `.profile-table` 的结构；日期值用 `split('-')` 渲染为三行/三段。
  2. 样式：新增上述类名与数值；确保与示例一致的圆角、颜色、阴影、间距。
  3. 计算属性：`completionPercent`；封装 `formatDateParts(selected.registeredAt)` 返回 `[YYYY, MM, DD]`。

## 兼容与响应式
- 右侧栏固定宽度 `320px`，随页面高度滚动
- 小屏（<960px）下 `grid` 改为单列，档案卡片置于列表下方；保持样式一致。

## 验收清单
- 标题与“编辑”位置、颜色、字号一致
- 外层米色背景与内层白卡圆角、阴影一致
- 头像大小与居中一致
- 信息行两列对齐、行间分割线一致
- “已完成课程 12节 (100%)” 数字可计算并与假数据吻合

请确认上述方案，确认后我将按此调整 `src/students.vue` 的模板与样式，实现一比一视觉还原。